<%= form_for(@task) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br />
    <%= f.text_area :descripcion %>
  </div>
  <div class="field">
    <%= f.label :fecha_inicia %><br />
    <%= f.datetime_select :fecha_inicia %>
  </div>
  <div class="field">
    <%= f.label :fecha_final %><br />
    <%= f.datetime_select :fecha_final %>
  </div>
  <div class="field">
    <%= f.label :user %><br />
<%= f.collection_select :user_id, User.all, :id, :nombre, :include_blank => false %>  

  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
